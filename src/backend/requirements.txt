# Web framework
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9

# Settings / validation
pydantic==2.9.2
pydantic-settings==2.5.2

# Database
sqlalchemy[asyncio]==2.0.35
asyncpg==0.29.0
psycopg2-binary==2.9.9

# Task queue
celery==5.4.0
redis==5.0.8

# ML / explainability — pinned to match ml/requirements.txt so the same
# model artifact loads identically in both the API/worker and the training
# pipeline.
torch==2.4.1
transformers==4.44.2
shap==0.46.0
numpy==1.26.4

# Testing
pytest==8.3.3
pytest-asyncio==0.24.0
httpx==0.27.2
